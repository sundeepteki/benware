function grid = initNoiseLearning(grid, expt)

seed = input('Input random seed -- this should probably be the penetration number: ');
initNoiseTokens(20, seed, 100, 200, 200, 200);
makeScreeningNoiseSequence;

id = 1;
makeAnoiseSequence(id);
makeBnoiseSequence;

